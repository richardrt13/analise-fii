{
  "name": "analise-gestao-fiis",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web",
    "functions"
  ],
  "scripts": {
    "dev": "pnpm -w run dev",
    "build": "pnpm -w run build",
    "lint": "pnpm -w run lint"
  }
}
